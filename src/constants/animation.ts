export const ANIMATION = {
  ARROW: {
    FLASH_DURATION: 1000, // One complete flash cycle in milliseconds
    MIN_OPACITY: 0.3,     // Minimum opacity during flash
    MAX_OPACITY: 1.0      // Maximum opacity during flash
  }
} as const;